services:
  - type: web
    name: stenico-api
    runtime: image
    image:
      url: Dockerfile
    dockerContext: ./backend
    disk:
      name: stenico-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: SQLITE_DB_PATH
        value: /data/stenico.db
