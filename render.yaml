services:
  - type: web
    name: stenico-backend
    runtime: image
    image:
      url: Dockerfile
    disk:
      name: stenico-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: SQLITE_DB_PATH
        value: /data/stenico.db
      - key: CORS_ORIGINS
        sync: false
